services:
  app:
    build: .
    ports:
      - "8473:8473"
    env_file:
      - .env.local
    environment:
      NOTES_DATA_DIR: /app/data/notes
    restart: unless-stopped
    volumes:
      - notes-data:/app/data

volumes:
  notes-data:
